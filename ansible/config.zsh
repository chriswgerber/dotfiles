
# Ansible Config File.
# export ANSIBLE_CONFIG="${DOTFILES_DIR}/ansible/ansible.cfg";

# Name of vault config file that's checked in the executed directory
export VAULT_CFG_FILE=".vault_cfg";

# Script for managing local ansible values.
export ANSIBLE_PASSWORD_SCRIPT="${DOTFILES_DIR}/ansible/libexec/security_vault_pass.sh";
